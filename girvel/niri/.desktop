#!/usr/bin/env zsh

if [ -z "$DISPLAY" ] && [ "$XDG_VTNR" -eq 1 ]; then
    sudo /opt/keyd_fix/restart  # prevent keyd race condition (a hack)
    export XDG_SESSION_TYPE=wayland
    export XDG_CURRENT_DESKTOP=niri
    exec dbus-run-session niri --session
fi
